Graphs 

    - used for recommendations on social networking, targeted advertisements
    - consists of a finite (and possibly mutable) set of vertices or nodes or points, together with a set of unordered pairs of these verticies for an undirected graph or a set of ordered pairs for a directed graph.
    - collection of nodes and connections between those nodes 
----------------------------------------------------------------------------------------------------------